-- Benchmark 1: Recursive Fibonacci
-- Tests: function call overhead, recursion depth

fn fib(n)
  if n <= 1 => return n
  fib(n - 1) + fib(n - 2)

result := fib(40)
print("fib(40) = " + str(result))
